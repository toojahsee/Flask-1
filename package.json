{
  "name": "workspace",
  "version": "1.0.0",
  "description": "Frontend for Flask with modern chessboard.js",
  "scripts": {
    "build": "echo 'No build needed for Flask + HTML setup.'"
  },
  "dependencies": {
    "vite": "^6.3.5"
  }
}